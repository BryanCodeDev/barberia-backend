PORT=3001
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=barber_trebol
JWT_SECRET=change-this-to-a-strong-secret-key
JWT_EXPIRES_IN=24h
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
WHATSAPP_API_KEY=
WHATSAPP_PHONE_NUMBER=
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password

# --- Railway ---
# When deploying to Railway, set these env vars in the Railway dashboard
# using the MySQL plugin references (Railway resolves ${{MySQL.MYSQLHOST}} etc.):
#   DB_HOST=${{MySQL.MYSQLHOST}}
#   DB_PORT=${{MySQL.MYSQLPORT}}
#   DB_USER=${{MySQL.MYSQLUSER}}
#   DB_PASSWORD=${{MySQL.MYSQLPASSWORD}}
#   DB_NAME=${{MySQL.MYSQLDATABASE}}
#
# Railway also auto-injects MYSQL_URL, MYSQLHOST, MYSQLPORT, MYSQLUSER,
# MYSQLPASSWORD, MYSQLDATABASE from the MySQL plugin. The application
# prioritizes MYSQL_URL, then DB_*, then MYSQL* env vars.
# Do NOT hardcode localhost/3306 here for production — use the plugin refs above.
